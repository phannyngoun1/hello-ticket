/**
 * Search Dialog Component
 * 
 * A generic, reusable search dialog component for selecting items from a list.
 */

export { SearchDialog } from './search-dialog';
export type { SearchDialogProps } from './types';
